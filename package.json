{
  "name": "minimax-code-plugins",
  "version": "0.1.0",
  "private": true,
  "description": "The community home for MiniMax Code Agent Plugins.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "create": "node scripts/create-plugin.mjs",
    "check": "npm run validate && npm test",
    "test": "node --test --test-concurrency=1",
    "validate": "node scripts/validate.mjs"
  },
  "license": "Apache-2.0"
}
