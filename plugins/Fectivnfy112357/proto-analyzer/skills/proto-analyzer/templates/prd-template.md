> ⚠️ **PRD 职责边界**：本文档只描述业务需求、字段含义（业务层面）、交互规则和异常场景。
> 禁止出现：数据库表名、SQL 类型（VARCHAR/INT）、API 路径、JSON 响应结构、技术实现细节。
> 字段描述示例：「姓名 — 必填，文本，最多 50 个字符，不可包含特殊符号」

# [页面名称] — 业务需求文档 (PRD)

## 1. 业务背景与痛点

> **描述当前业务面临的问题，为什么需要这个页面/功能。**
> *示例：客服目前需要手动查询多个系统来核对订单状态，平均处理一单耗时 8 分钟，导致客户等待时间过长。*

**业务背景：** [由用户提供的上下文填充]

**预期目标：** [该功能的预期效果，如：将处理时间缩短至 2 分钟以内]

---

## 2. 角色与使用场景

| 角色 | 职责 | 使用该页面的场景 |
|------|------|-----------------|
| [如：运营人员] | [如：日常订单审核] | [如：每天上班后批量审核待处理订单] |
| [如：C端用户] | [如：下单/查询] | [如：购买后查看订单进度] |

---

## 3. 核心业务流程

> **用文字描述步骤，标注关键分支。**

```
[入口] → [页面A：列表筛选] → [选择记录] → [页面B：详情查看]
                              ↓
                      [点击编辑] → [页面C：表单填写] → [提交校验] → [成功/失败反馈]
```

**关键业务规则：**
1. [规则 1，如：只有待审核状态的订单可以编辑]
2. [规则 2]
3. [规则 3]

---

## 4. 页面模块拆解

### 4.1 筛选区

| 筛选项 | 字段名 | 控件类型 | 默认值 | 可选值来源 |
|--------|--------|---------|--------|-----------|
| [如：订单状态] | [status] | [下拉选择] | [全部] | [字典接口 / 枚举] |
| ... | ... | ... | ... | ... |

### 4.2 数据列表区

| 列名 | 字段名 | 数据类型 | 可排序 | 可筛选 | 备注 |
|------|--------|---------|--------|--------|------|
| [如：订单号] | [order_no] | [字符串] | [是] | [是] | [点击跳转详情] |
| ... | ... | ... | ... | ... | ... |

### 4.3 操作区

| 按钮/操作 | 触发条件 | 前置校验 | 后续行为 |
|----------|---------|---------|---------|
| [如：批量审核] | [勾选≥1条记录] | [状态必须为"待审核"] | [弹窗确认 → 提交 → 刷新列表] |
| ... | ... | ... | ... |

### 4.4 [其他模块：表单区/图表区/...]

> 根据原型实际模块增减。

---

## 5. 字段与交互规则

### 5.1 输入项约束

| 字段名 | 标签 | 数据类型 | 必填 | 长度/范围 | 校验规则 | 业务约束 |
|--------|------|---------|------|----------|---------|---------|
| [如：手机号] | [联系电话] | [字符串] | [是] | [11位] | [手机号正则] | [必须为订单创建人的手机] |
| [如：金额] | [退款金额] | [数字] | [是] | [0.01~订单总额] | [两位小数] | [不可超过原订单实付金额] |
| ... | ... | ... | ... | ... | ... | ... |

### 5.2 操作反馈规则

| 操作 | 反馈类型 | 反馈内容 | 后续状态 |
|------|---------|---------|---------|
| [提交表单] | [成功] | ["提交成功，审核结果将在24小时内通知"] | [返回列表页，刷新数据] |
| [提交表单] | [失败-校验不通过] | [高亮错误字段 + 具体提示] | [停留在当前页，保留已填内容] |
| [重复点击提交] | [防重] | [按钮置灰 + loading 态] | [请求完成前不可再次点击] |
| ... | ... | ... | ... |

---

## 6. 异常场景定义

| 异常类型 | 触发条件 | 页面表现 |
|---------|---------|---------|
| 数据为空 | 列表查询无结果 | [显示空状态占位图 + "暂无数据"文案 + 清空筛选按钮] |
| 网络超时 | 接口请求 > 10s 未响应 | [Toast 提示"网络异常，请稍后重试" + 重试按钮] |
| 权限不足 | 当前角色无权操作 | [按钮隐藏或置灰 + Tooltip 提示"需要XX权限"] |
| 并发冲突 | 记录已被他人修改 | ["该记录已被修改，请刷新后重试"] |
| 会话过期 | Token 失效 | [跳转登录页] |
